<!--<view class='mask2' wx:if='{{show}}' bindtap='clickMask'>-->
<form bindsubmit="confirm" report-submit='true' >
    <view wx:if='{{show}}' class='modal-content' style="margin:atuo;">
        <slot name="slot1"></slot>
        <scroll-view scroll-y class='main-content'>
            <slot name="slot2"></slot>
        </scroll-view>
        <view class='modal-btn-wrapper'>
            <!-- <view class='cancel-btn' style='color:rgba(7,17,27,0.6)' bindtap='cancel'>取消</view> -->
            <!-- <view class='confirm-btn'>确认拼车</view> -->
            <button form-type="submit"  class='confirm-btn'>确认拼车</button>
        </view>
    </view>
</form>
<!--</view>-->
